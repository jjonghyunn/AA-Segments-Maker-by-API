# Auth credentials — never commit
*aanalytics_auth*.json
*auth*.json

# Result CSVs (run-specific outputs)
result_*.csv
test_result_*.csv
compare_panel_segments_*.csv
users_*.csv

# Debug dumps
_debug_*.json
_debug_*.csv

# Backups / local work copies — never commit (may contain unsanitized values)
*backup*/
*backup*
old_local_backup/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Archived old versions - never commit (may contain unsanitized values)
old/
